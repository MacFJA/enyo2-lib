enyo.kind({
	name: "layout.FlexableRows",
	layoutKind: "layout.FlexableRowsLayout",
	/** By default, items in rows stretch to fit horizontally; set to true to
		avoid this behavior. */
	noStretch: false
});