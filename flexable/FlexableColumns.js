enyo.kind({
	name: "layout.FlexableColumns",
	layoutKind: "layout.FlexableColumnsLayout",
	/** By default, items in columns stretch to fit vertically; set to true to
		avoid this behavior. */
	noStretch: false
});
